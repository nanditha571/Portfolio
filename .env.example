# Server
PORT=3001
NODE_ENV=development

# Frontend URL for CORS in production
# Vercel automatically provides VERCEL_URL, but you can override with FRONTEND_URL
FRONTEND_URL=http://localhost:5173

# Session secret (optional - currently using in-memory sessions)
# SESSION_SECRET=your-secret-here
